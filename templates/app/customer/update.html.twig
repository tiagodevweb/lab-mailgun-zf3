{% extends '@layout/adminlte.html.twig' %}

{% block title %}
    Editar Contato {{ parent() }}
{% endblock %}

{% block page_header %}
    Editar Contato {{ form.getObject().name }}
{% endblock %}

{% block content %}

    {% include '@app/customer/_form-elements.html.twig' %}

    {% set submit = form.get('submit') %}
    {% do submit.setAttribute('class','btn btn-primary').setLabel('Atualizar') %}

    {% do form.setAttribute('action',url('customer.update', {id: form.getObject().id })) %}
    {% do form.prepare() %}

        <div class="box">

            {{ form().openTag(form) | raw}}

            {{ formHidden(form.get('_method')) }}

            <div class="box-body">

                {% include '@app/customer/_form-body.html.twig' %}

            </div>

            <div class="box-footer">
                {{ formRow(submit) }}
            </div>

            {{ form().closeTag() | raw}}

        </div>

{% endblock %}
